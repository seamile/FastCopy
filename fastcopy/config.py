SERVER_ADDR = ('127.0.0.1', 7523)
CHUNK_SIZE = 64 * 1024  # 全量传输的数据块大小 (字节)
SSH_MUX = 2
TIMEOUT = 60 * 5  # 全局超时时间 (握手、接收空闲等)
LEN_HEAD = 9  # flag(1B) + chksum(4B) + length(4B)

# 连接空闲看门狗阈值: 一个连接在发送方向上停滞超过该秒数即强制关闭该连接。
IDLE_TIMEOUT = 30

# 分块确认超时: 发送端超过该秒数未收到某个分块的 ACK 就重传该分块。
# 丢包率高的网络下需要更短的超时以便更快重传。
ACK_TIMEOUT = 10

# ---- 增量同步 (rsync 算法) 参数 ----
SIG_BLOCK_SIZE = 8192  # 签名块大小 (字节)，越小增量粒度越细但签名越大
DELTA_MAX_SIZE = 256 * 1024 * 1024  # 超过该大小的文件不做增量，直接全量传输
MAX_LIT = 64 * 1024  # 增量同步中单个字面量报文的最大字节数
